# .DS_Store files should never be committed
.DS_Store

# .class files will never be committed
*.class*

# /bin directory shouldn't be committed
/bin/

#.project files will never be committed
.project

.classpath
